{
  "name": "scittle-emmy-viewers-demo",
  "version": "0.0.0",
  "devDependencies": {
    "vite": "^5.4.8",
    "vite-plugin-handlebars": "^2.0.0",
    "wrangler": "^3.80.4"
  },
  "private": true,
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "publish": "wrangler pages deploy --project-name=ev-demo --branch=main --commit-dirty=true ./dist"
  },
  "type": "module"
}
